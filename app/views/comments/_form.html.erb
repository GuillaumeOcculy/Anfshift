<%= form_for([@shift, @shift.comments.build]) do |f| %>

  <div class="form-group">
    <%= f.label :content %>
    <%= f.text_area :content, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.submit nil, class: 'btn btn-default' %>
  </div>


<% end %>
